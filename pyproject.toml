[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "energy-forecast-optimization"
version = "0.1.0"
description = "AI-driven energy consumption forecasting and optimization system"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

